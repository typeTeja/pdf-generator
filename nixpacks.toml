# Nixpacks configuration for Coolify deployment
[phases.setup]
aptPkgs = [
    "libpango-1.0-0",
    "libpangoft2-1.0-0",
    "libharfbuzz0b",
    "libcairo2",
    "libglib2.0-0",
    "libffi-dev",
    "shared-mime-info"
]

[start]
cmd = "uvicorn app.main:app --host 0.0.0.0 --port 8000"
